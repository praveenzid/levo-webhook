{
  "name": "levo-webhook",
  "version": "1.0.0",
  "description": "Webhook endpoint for Levo.so to Slack integration",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}
